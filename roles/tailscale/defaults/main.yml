---
# =============================================================================
# tailscale role — install Tailscale and enable tailscaled on Linux. Darwin
# installs Tailscale.app through Homebrew; authenticate it interactively after
# installation.
tailscale_service: tailscaled
tailscale_cask_macos: tailscale-app
