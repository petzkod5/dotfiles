---
# Galaxy dependencies. Install with `dotfiles update`, or directly:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general
    version: ">=13.0.0"
  - name: ansible.posix
    version: ">=2.0.0"
  - name: https://github.com/petzkod5/omp-ansible-collection.git
    type: git
    version: v0.2.0

roles: []
